#!/usr/bin/env sh
#
# Run the slower correctness checks before code leaves the machine:
# static analysis + the full test suite. Formatting is already handled at commit.

set -e

echo "→ PHPStan"
composer stan

echo "→ Tests"
php artisan test

exit 0
